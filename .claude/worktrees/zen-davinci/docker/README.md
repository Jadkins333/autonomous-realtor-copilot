# Docker Notes

Service Dockerfiles:
- API: `apps/api/Dockerfile`
- Web: `apps/web/Dockerfile`

Compose file:
- `docker-compose.yml`

Containers include seed data mounts and auto-bootstrap behavior for demo mode.
