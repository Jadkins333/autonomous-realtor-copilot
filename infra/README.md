# Infrastructure Notes

This directory is reserved for deployment overlays outside local Docker Compose.

The MVP runtime in this repository is fully local-first and starts via:

```bash
docker compose up --build
```
