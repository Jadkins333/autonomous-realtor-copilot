# Synthetic Seed Data

These files power demo mode with no paid credentials:

- `parcels.json`
- `permits.json`
- `flood_zones.json`
- `pois.json`
- `transit_stops.json`
- `mortgage_rates.json`

The API loader checks `/seed` first, then local fallback paths, and records provenance for every ingested object.
