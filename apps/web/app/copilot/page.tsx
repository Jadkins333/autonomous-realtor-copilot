"use client";  import { useEffect, useMemo, useState } from "react"; import { useSession } from "next-auth/react"; import Link from "next/link";  import { useRequireAuth } from "@/components/auth-guard"; import { ProvenanceDrawer } from "@/components/provenance-drawer"; import { SiteShell } from "@/components/site-shell"; import { Badge } from "@/components/ui/badge"; import { Button } from "@/components/ui/button"; import { Card, CardDescription, CardTitle } from "@/components/ui/card"; import { Input } from "@/components/ui/input"; import { apiFetch } from "@/lib/api"; import { COPILOT_COMMANDS } from "@/lib/commands";  type CopilotResponse = {   status: string;   text: string;   data?: any;   trace?: any;   missing_inputs?: string[]; };  type ChatItem = {   role: "user" | "assistant";   text: string;   payload?: CopilotResponse; };  type AgentRow = {   key: string;   name: string;   description: string; };  export default function CopilotPage() {   const { status } = useRequireAuth();   const { data: session } = useSession();   const [query, setQuery] = useState(COPILOT_COMMANDS[0]);   const [messages, setMessages] = useState<ChatItem[]>([]);   const [agents, setAgents] = useState<AgentRow[]>([]);   const [loading, setLoading] = useState(false);    const isOffline = typeof navigator !== "undefined" ? !navigator.onLine : false;    async function submitCommand() {     if (!session || !query.trim() || isOffline) return;     const message = query.trim();     setMessages((prev) => [...prev, { role: "user", text: message }]);     setLoading(true);     try {       const result = await apiFetch<CopilotResponse>("/copilot/chat", session?.apiToken, {         method: "POST",         body: JSON.stringify({ message })       });       setMessages((prev) => [...prev, { role: "assistant", text: result.text, payload: result }]);     } catch (error) {       const detail = error instanceof Error ? error.message : "Copilot request failed";       setMessages((prev) => [...prev, { role: "assistant", text: detail }]);     } finally {       setLoading(false);     }   }    const latestAssistantPayload = useMemo(() => {     const assistant = [...messages].reverse().find((item) => item.role === "assistant" && item.payload);     return assistant?.payload;   }, [messages]);    useEffect(() => {     if (!session) return;     apiFetch<AgentRow[]>("/copilot/agents", session?.apiToken)       .then(setAgents)       .catch(() => setAgents([]));   }, [session]);    if (status !== "authenticated") {     return null;   }    return (     <SiteShell>       <Card className="mb-4">         <CardTitle>Copilot</CardTitle>         <CardDescription className="mt-1">           Deterministic routing with trace metadata and provenance.         </CardDescription>         <div className="mt-2">           <Link className="text-sm text-accent underline" href="/copilot/agents">             View all copilot agents           </Link>         </div>         <div className="mt-4 flex flex-col gap-2 md:flex-row">           <Input value={query} onChange={(event) => setQuery(event.target.value)} />           <Button disabled={loading || isOffline} onClick={submitCommand}>             {loading ? "Running..." : isOffline ? "Offline" : "Run"}           </Button>         </div>         <div className="mt-3 flex flex-wrap gap-2">           {COPILOT_COMMANDS.map((cmd) => (             <Button key={cmd} variant="outline" onClick={() => setQuery(cmd)}>               {cmd}             </Button>           ))}         </div>       </Card>        <Card>         <CardTitle className="mb-3">Chat</CardTitle>         <div className="space-y-3">           {messages.length === 0 ? <p className="text-sm text-muted-foreground">Run a command to start.</p> : null}           {messages.map((item, index) => (             <div               key={`${item.role}-${index}`}               className={item.role === "user" ? "ml-auto max-w-[85%]" : "mr-auto max-w-[90%]"}             >               <div                 className={                   item.role === "user"                     ? "rounded-xl bg-muted p-3 text-sm"                     : "rounded-xl border bg-card p-3 text-sm"                 }               >                 <p>{item.text}</p>                 {item.payload?.trace?.selected_agent ? (                   <div className="mt-2">                     <Badge>agent: {item.payload.trace.selected_agent}</Badge>                   </div>                 ) : null}                 {item.payload?.trace ? (                   <details className="mt-2 text-xs">                     <summary className="cursor-pointer text-muted-foreground">Trace</summary>                     <pre className="mt-2 overflow-auto rounded-xl bg-muted p-2">                       {JSON.stringify(item.payload.trace, null, 2)}                     </pre>                   </details>                 ) : null}               </div>             </div>           ))}         </div>         {latestAssistantPayload?.data ? (           <div className="mt-3">             <ProvenanceDrawer               formula={latestAssistantPayload?.data?.formula_markdown || "No formula"}               inputs={                 latestAssistantPayload?.data?.inputs || {                   missing_inputs: latestAssistantPayload?.missing_inputs || []                 }               }               provenance={                 {                   metric_provenance: latestAssistantPayload?.data?.provenance || {},                   trace: latestAssistantPayload?.trace || {}                 }               }             />           </div>         ) : null}       </Card>       <Card className="mt-4">         <CardTitle className="mb-2">Available Agents</CardTitle>         <ul className="space-y-2 text-sm">           {agents.map((agent) => (             <li key={agent.key} className="rounded-lg border bg-card p-2">               <p className="font-medium">{agent.name}</p>               <p className="text-muted-foreground">{agent.description}</p>             </li>           ))}         </ul>       </Card>     </SiteShell>   ); }